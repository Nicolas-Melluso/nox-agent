# Configs

Runtime configuration begins after v0.1.

For now, a workspace initialized with `nox init` only creates:

```text
.nox/
  system.prompt.md
```

The installed Nox engine owns defaults, schemas, policies and adapters.
